%h1 Listing Books

%table
  %thead
    %tr
      %th Title
      %th User
      %th(colspan=3)

  %tbody
    - @books.each do |book|
      = BookPresenter.new(book, self).for_index(current_user)
%br

#{link_to 'New Book', new_book_path}
